Users: {{usersCount}}
{{#link-to 'users.create' class='create-btn'}}Add User{{/link-to}}
<ul class="users-listing">
  {{#each user in controller}}
    <li>
      {{#link-to "user" user}}
        {{user.name}}
      {{/link-to}}
    </li>
  {{else}}
    <li>no users… :-(</li>
  {{/each}}
</ul>

{{outlet}}

{{#link-to 'index' }} Go back to index {{/link-to}}
